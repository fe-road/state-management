import HeaderUserMenu from './HeaderUserMenu';

const Header = () => {
    return (
        <div>
            <span>Application Header</span>
            <HeaderUserMenu />
        </div>
    );
};

export default Header;